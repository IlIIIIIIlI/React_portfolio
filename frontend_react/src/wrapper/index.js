// we don't want to implement some components every time in each sub-pages,
// that's why we need wrapper
import AppWrap from './AppWrap';
import MotionWrap from './MotionWrap';

export {
  AppWrap,
  MotionWrap,
};
